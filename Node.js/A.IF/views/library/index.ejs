<!DOCTYPE html>
<html>
  <head>
    <% include ../partials/head %>
  </head>
  <body>
    <% include ../partials/nav %>

    <div class="container post post-index">
      <% include ./partials/header %>

      <div class="buttons">
        <% if(isAuthenticated){ %>
          <a class="btn btn-default" href="/library/new">New</a>
        <% } %>
      </div>

      <table class="table table-striped library">
        <thead>
          <tr>
            <th class="title">Title</th>
            <th class="author">Author</th>
            <th class="date">Date</th>
          </tr>
        </thead>
        <tbody>
          <% if(library == null || library.length == 0){ %>
            <tr>
              <td class="noData" colspan=100> There is no data to show :( </td>
            </tr>
          <% } %>
          <% library.forEach(function(post) { %>
            <tr>
              <td class="title">
                <a href="/library/<%= post._id %>"><div class="ellipsis"><%= post.title %></div></a>
                <span class="comments">(<%= post.comments.length %>)</span>
              </td>
              <td class="author">
                <div class="ellipsis"><%= post.author ? post.author.username : "" %></div>
              </td>
              <td class="date">
                <%= post.createdDate %>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>

    </div>  <!-- container end -->
  </body>
</html>
